APP_ENV=production
HOST=127.0.0.1
PORT=8001
RELOAD=false

# SQLite (async). Файл по умолчанию: backend/app.db
# DATABASE_URL=sqlite+aiosqlite:////var/www/proffi/backend/app.db

JWT_SECRET=change-this-to-a-long-random-secret
ENABLE_TEST_SEED=false

# Comma-separated list of allowed frontend origins
CORS_ORIGINS=https://example.com,https://www.example.com

# Optional: regex for subdomains if needed
# CORS_ORIGIN_REGEX=https:\/\/.*\.example.com

# Optional object storage integration
EMERGENT_LLM_KEY=

# Admin panel API (header X-Admin-Token)
ADMIN_TOKEN=admin
